{
  "name": "checkm8",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload\" \"cd frontend && npm run dev -- --host 0.0.0.0 --port 5173\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
